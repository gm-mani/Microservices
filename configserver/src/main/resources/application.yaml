spring:
  application:
    name: configserver
  profiles:
    active: git
  # active: native
  cloud:
    config:
      server:
        # native:
        # searchLocations: classpath:/config
        # searchLocations: "file:///C:/Users/G M MANI/OneDrive/Desktop/SpringBoot/Udemy/Config"
        git:
          uri: https://github.com/gm-mani/config-data.git
          force-pull: true
          timeout: 5
          clone-on-start: true
          default-label: main

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8071
management:
  endpoints:
    web:
      exposure:
        include: "*"
